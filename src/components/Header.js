import React from "react";

const Header = () => (
	<div className="container-fluid">
		<div className="jumbotron">
			<h1>Clicky Game</h1>
			<p>Click on an image to earn point, but click an image more than onece.</p>
		</div>
	</div>
	);

export default Header;